function fo(...arr) {
    if (typeof arr[0] === "string") {
        // TODO
        return;
    }
    if (typeof arr[0] === "number") {
        // TODO
        return;
    }
    if (arr.length === 2) {
        // TODO
        return;
    }
    if (arr.length === 3) {
        // TODO
        return;
    }
}

fo("Pepe");
fo(22);
fo(22, 34);
fo(22, 34, 78);
